Drop your 3 app screenshots here with these EXACT filenames:

  dashboard.png
  syllabus-map.png
  revision-timer.png

(PNG or JPG both work — if you use .jpg, update the file extension
inside components/AppPreview.tsx to match.)

Recommended size: roughly 1080 x 2340 px (a standard phone screenshot,
9:16 portrait), so it fills the frame nicely without stretching.

Once the files are here, the placeholders on the homepage will
automatically be replaced by your real screenshots — no other code
changes needed.
